<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Quyền truy cập Internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApp"
        tools:targetApi="31">


        <!-- MainActivity (ứng dụng khởi chạy) -->
        <activity
            android:name=".Activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AdminMainActivity -->
        <activity android:name=".Activity.AdminMainActivity" />

        <!-- Các Activity khác -->
        <activity android:name=".Activity.AdDetailsActivity" />
        <activity android:name=".Activity.MovieDetailsActivity" />

        <!-- Các Activity quản lý -->
        <activity android:name=".Activity.QuanLyRapActivity" />
        <activity android:name=".Activity.QuanLyPhimActivity" />
        <activity android:name=".Activity.QuanLyPhimDetailActivity" />
        <activity android:name=".Activity.QuanLyPhongActivity" />
        <activity android:name=".Activity.QuanLyQuangCaoActivity" />
        <activity android:name=".Activity.QuanLyQuangCaoDetailActivity" />
        <activity android:name=".Activity.QuanLyBacThanhVienActivity" />
        <activity android:name=".Activity.QuanLyNguoiDungActivity" />
        <activity android:name=".Activity.QuanLySuatActivity" />
        <activity android:name=".Activity.ThongKeActivity" />
    </application>
</manifest>
